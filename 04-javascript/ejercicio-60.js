// sesion 6

// creamos array
const lista = [];

// llenamos array con los numeros del 1 al 20
for( let i = 1; i <= 20; i++ ) { lista.push(i); }

// mostramos los elementos del en consola
lista.forEach( elemento => console.log(elemento) );